# ExcelToSheetAnnotation

> 🔗 Импорт и автообновление Excel-таблиц как текстовых аннотаций на листах Revit.

Плагин для Autodesk Revit, который связывает листы Revit с листами книги Excel и
преобразует диапазон ячеек в “немую” таблицу из текстовых аннотаций на листе.
При каждом сохранении Excel файл автоматически синхронизируется с Revit.

---

## ✨ Основные возможности

- 📑 **Связка Excel ↔ Revit по имени листа**  
  Имя листа Excel должно совпадать с *Sheet Number* в Revit  
  (например, лист Excel `A101` → лист Revit `A101`).

- 🧩 **Импорт диапазона ячеек**  
  Один общий диапазон (например, `A1:H20`) применяется ко всем листам книги.

- 🔁 **Автоматическое обновление**  
  Плагин отслеживает изменения Excel-файла (через FileSystemWatcher)  
  и при сохранении пересобирает таблицу на соответствующих листах.

- ✏️ **Текстовые аннотации вместо настоящей таблицы**  
  Каждая ячейка Excel становится отдельной TextNote на листе Revit.  
  Это удобно для “немых” спецификаций, примечаний, текстовых таблиц и т.п.

- ⚙️ **Окно настроек**  
  Простое окно: выбор Excel-файла + ввод диапазона (`A1:H20`).

- 🛡 **Встроенный trial и активация**  
  7-дневный пробный период без ограничений.  
  Далее требуется активация по лицензионному ключу.

---

## 🧠 Как это работает

1. **Подготовьте Excel**

   - Один файл Excel.
   - Несколько листов (вкладок) в книге.
   - Каждому листу задайте имя, совпадающее с *Sheet Number* в Revit.  
     Например:
     - Excel: `A101`, `A102`, `A201`
     - Revit: листы с номерами `A101`, `A102`, `A201`.

   - Определите диапазон, который будет использоваться как таблица,  
     например: `A1:H20`.

2. **Подготовьте листы в Revit**

   - Создайте/переименуйте листы так, чтобы их *Sheet Number* совпадали  
     с именами листов Excel.

3. **Запустите плагин**

   - В Revit на ленте появится вкладка/панель с кнопкой, например **Excel Sync**.
   - Нажмите кнопку → выберите Excel-файл → укажите диапазон (`A1:H20`).

4. **Импорт**

   - Для каждого листа Excel плагин ищет лист Revit с таким же номером.
   - На найденных листах создаётся “таблица” из текстовых аннотаций.

5. **Автообновление**

   - При каждом сохранении Excel:
     - плагин замечает изменение файла;
     - обновляет содержимое “таблиц” на соответствующих листах Revit.

---

## 📦 Установка

### Требования

- Autodesk Revit **2025** (x64)  
  > При необходимости поддержку других версий можно добавить в будущих релизах.
- Microsoft Excel, установленный на той же машине.
- Windows 10/11 x64.
- Права администратора для установки.

### Шаги установки

1. Перейдите на страницу релизов:  
   👉 **[Скачать последнюю версию](../../releases/latest)**

2. Скачайте `ExcelToSheetAnnotation_Setup.exe`.

3. Запустите установщик от имени администратора и следуйте инструкциям мастера.

4. Перезапустите Revit.  
   В ленте появится новая вкладка/панель с кнопкой запуска плагина.

### Обновление

1. Закройте Revit.
2. Скачайте новый `Setup` из раздела Releases.
3. Запустите его – предыдущая версия будет обновлена автоматически.

### Удаление

- Используйте стандартное:
  - **Параметры → Приложения → Приложения и возможности** (Windows 10/11)  
  - или “Программы и компоненты”.
- Найдите **ExcelToSheetAnnotation** и нажмите **Удалить**.  
  Установщик удалит DLL и `.addin`-файл из папок Revit.

---

## ⚠️ Ограничения текущей версии

- Один общий диапазон (`A1:H20` и т.п.) для всех листов книги.
- Плагин создаёт только текстовые аннотации (без линий и рамок таблицы).
- Положение “таблицы” на листе фиксировано (первая версия).
- Поддерживается только Revit 2025 (остальные версии будут добавлены позже).

---

## 🧪 Trial и лицензирование

- При первом запуске автоматически активируется **7-дневный trial**.
- В trial-режиме доступны **все функции без ограничений**.
- После окончания trial:
  - основные функции блокируются;
  - появляется окно с предложением ввести лицензионный ключ.

> 🔑 1 лицензия = 1 рабочее место (пользователь/машина).

### Как получить лицензию

- Email: `danrogulin@gmail.com`
- Telegram: `https://t.me/sadblueses`
- GitHub: `https://github.com/Akkhazriel`

Формат оплаты, стоимость и условия обновлений описаны в отдельном документе.

---

## 🗺 Roadmap

Планы на ближайшие версии:

- [ ] Поддержка нескольких версий Revit (2023–2026).
- [ ] Настраиваемое положение таблицы на листе.
- [ ] Несколько диапазонов для разных зон листа.
- [ ] Отрисовка рамки/сеток таблиц (линии).
- [ ] Импорт простого форматирования (жирный, выравнивание).
- [ ] Больше опций по обновлению (ручной/авто режимы).

Если у вас есть пожелания или идеи — создавайте [Issue](../../issues) или пишите в личку.

---

## 🛠 Поддержка и обратная связь

- Нашли баг?  
  Создайте issue в разделе **[Issues](../../issues)** с:
  - версией Revit;
  - версией плагина;
  - кратким описанием;
  - шагами для воспроизведения;
  - скриншотами/примером Excel-файла (по возможности).

- Нужна кастомизация под ваш офис / шаблон?  
  Свяжитесь по контактам выше.

---

## 🌍 English summary

**ExcelToSheetAnnotation** is a Revit add-in that links Excel worksheets to
Revit sheets by sheet number and renders a given cell range as text notes on
each sheet. Every time you save the Excel file, the add-in automatically
updates the corresponding “dumb table” on Revit sheets.

- One Excel workbook, multiple worksheets (`A101`, `A102`, …).
- Worksheet name = Revit sheet number.
- Single cell range (e.g. `A1:H20`) imported as text notes.
- Automatic updates on Excel save (file watcher).
- 7-day full-featured trial, then license activation.

👉 Download: [Latest release](../../releases/latest)  
👉 Issues / feature requests: [GitHub Issues](../../issues)

---
